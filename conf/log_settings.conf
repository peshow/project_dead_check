{
  thread {
     file1 {
       project = "a.log"
       counts_send = 1
       log_path = "/data/log/a.log"
       recipients = "526399002@qq.com"
       patterns = "error"
       auto_cut = "True"
       scheduler = {
           trigger: interval
           seconds: 10
       }
     }
     file2 {
       project = "b.log"
       counts_send = 1
       log_path = "/data/log/b.log"
       recipients = "526399002@qq.com"
       patterns = "error"
       auto_cut = "True"
       scheduler = {
           trigger: interval
           seconds: 5
       }
     }
  }
  subj_body = {
    subject = "PROBLEM: [{}]发现错误"
    body = "{}"
  }
  include file("conf.d/test.conf")
}
