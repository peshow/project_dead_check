{
  thread {
    logfile = ["/data/log/a.log", "/data/log/b.log"]
  }
  email {
    subject = "Error Message"
    smtp = smtp.163.com  // smtp服务器的URL地址
    src = ""             // 邮箱账号，也是发送邮件的用户
    recipients = ""      // 收件人列表，以逗号隔开
    password = ""        // 邮箱密码 
  }
  include file("conf.d/test.conf")
}
