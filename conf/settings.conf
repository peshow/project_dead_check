{
  thread {
     file1 {
       project = "a.log"
       counts_send = 1
       log_path = "/data/log/a.log"
       recipients = "526399002@qq.com"
       scheduler = {
         trigger = interval
         seconds: 15
        # hour = 15
        # minute = 5
         }
     }
     file2 {
       project = "b.log"
       counts_send = 1
       log_path = "/data/log/b.log"
       recipients = "526399002@qq.com"
       scheduler = {
           trigger: interval
           seconds: 5
          }
     }
  }
  email {
    subject = "Error Message"
    smtp = smtp.163.com  // smtp服务器的URL地址
    port = 25
    src = "qiubiwu1234@163.com"             // 邮箱账号，也是发送邮件的用户
    recipients = "526399002@qq.com"      // 收件人列表，以逗号隔开
    password = "6620904a"        // 邮箱密码 
  }
  include file("conf.d/test.conf")
}
